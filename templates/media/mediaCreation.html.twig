{% extends 'generic/base.html.twig' %}
{% block title %}Ajouter media{% endblock %}
{% block body %}
    <h1> Ajouter un media au trick: {{ trickName }}</h1>
    <div class="container" id="trick_creation">
        <div class="row d-flex justify-content-center">
            <div class="col-10">
                {{ form_start(mediaForm) }}
                {{ form_row(mediaForm.url) }}
                {{ form_row(mediaForm.mediaType) }}
                {{ form_row(mediaForm.isMain) }}
                <div class="text-center">
                    <button type="submit" class="btn btn-primary">Ajouter media</button>
                </div>
                {{ form_end(mediaForm) }}
            </div>
        </div>
    </div>
    <div class="container" id="trick_creation">
        <!-- Medias cards -->
        <div class="row d-flex justify-content-center">
            <div class="col-2">

            </div>
        </div>
    </div>

{% endblock %}

